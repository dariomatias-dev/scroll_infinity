name: scroll_infinity
description: Provides infinite scrolling functionality, simplifying its integration.
version: 0.3.1
homepage: https://github.com/dariomatias-dev/scroll_infinity

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
